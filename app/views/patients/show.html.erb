<h2><%= @patient.name %></h2>
<p>
Appointments:
  <% @patient.appointments.each do |appointment| %>
    <%= link_to appointment.doctor.name, doctor_path(appointment.doctor.id) %> -
    <%= appointment.appointment_datetime.strftime("%B %-d, %Y at %k:%M ") %>
  <% end %>

  </p>