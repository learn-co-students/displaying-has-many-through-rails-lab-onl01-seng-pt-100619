<h1>Doctor: <%= @doctor.name %></h1>
<%= @doctor.department %>

<h4>Appointments:</h4>
<% @doctor.appointments.each do |appt| %>
    <p>Date/Time: <%= appt.print_date_time %></p>
    <p>Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %></p>
<% end %>