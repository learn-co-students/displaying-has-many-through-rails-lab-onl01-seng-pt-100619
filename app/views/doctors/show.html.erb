<h1><%= @doctor.name %></h1>
<h2>Appointments:</h2>
<ul>
    <% @doctor.appointments.each do |appointment| %>
        <li><%= link_to appointment.patient.name, patient_path(appointment.patient)%> at <%= appointment.pretty_date %></li>
    <% end %>
</ul>